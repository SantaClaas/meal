import materialTailwind from "@claas.dev/material-tailwind";
// Tailwind CSS 4 can't call functions in the CSS file so we need to basically create it here
export default materialTailwind({
  source: "#0f172a",
});
